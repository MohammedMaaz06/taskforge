apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskforge.fullname" . }}
  labels:
    {{- include "taskforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskforge.selectorLabels" . | nindent 4 }}

