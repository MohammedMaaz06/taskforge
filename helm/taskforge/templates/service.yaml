apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskforge.fullname" . }}
  labels:
    app: {{ include "taskforge.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "taskforge.name" . }}

